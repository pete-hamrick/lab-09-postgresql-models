import pool from '../utils/pool';

export default class Characters {
    constructor(row) {
        this.id = row.id;
        this.name = row.name;
        this.height = row.height;
    }

    //insert
}
